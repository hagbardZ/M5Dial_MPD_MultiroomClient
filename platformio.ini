; =====================================================================
;  M5Dial MPD client - PlatformIO project
;
;  Hardware  : M5Stack M5Dial  (ESP32-S3-FN8, 1.28" round GC9A01 LCD)
;  UI        : M5GFX (direct drawing, no LVGL)
;  Libraries : m5stack/M5Dial (M5Unified + M5GFX based)
;
;  Build     : pio run
;  Upload    : pio run -t upload   (USB serial)
;  Monitor   : pio device monitor
; =====================================================================
[platformio]
default_envs = m5dial

[env:m5dial]
platform = espressif32
board = m5stack-dial
framework = arduino
monitor_speed = 115200
upload_speed = 921600

lib_deps =
    m5stack/M5Dial@^1.0.3

build_flags =
    -DCORE_DEBUG_LEVEL=1
    -DCONFIG_ARDUINO_LOOP_STACK_SIZE=16384

; Uncomment and adjust your serial port if auto-detection fails:
; upload_port = /dev/ttyACM0